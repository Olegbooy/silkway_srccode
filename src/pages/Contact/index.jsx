import React, { memo } from 'react';
import { Helmet } from "react-helmet";

const Contact = () => {
	return (
		<Helmet>
			<title>Silkway Contacts</title>
		</Helmet>
	);
};

export default memo(Contact);
